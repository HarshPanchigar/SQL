create database collage;
use collage;
create table students(id int primary key,name varchar(50), age int not null);

insert into students values(1,"harsh",23);

insert into students values(2,"amrut",23);

select * from students;
